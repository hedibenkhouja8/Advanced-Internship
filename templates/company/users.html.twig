{% extends 'base.html.twig' %}
{% block body %}
<style>
a{color:black;}
h2{color:#485785}
.b{margin-left:700px;
padding-right:400px;}
.container {
  max-width: 1000px;
  margin-left: auto;
  margin-right: auto;
  padding-left: 10px;
  padding-right: 10px;
}

</style>
<section id="main-content">
	<section class="wrapper">
		<div class="table-agile-info2">
   
     <h2>These are the other users:</h2>
       <a href="{{path("security_registration")}}" class="btn btn-outline-info btn-lg float-right" ><i class="fas fa-user-plus"></i>Add User</a>
<br><br><form method="POST">
<input type="text" name="name">
<input type="button" value="search"/>

</form>
 <div class="table-inbox-wrap ">
                            <table class="table table-inbox table-hover">
                               <thead>
    <tr>
      <th scope="col">Favorites</th>
      <th scope="col">User Name</th>
      <th scope="col">Email</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
                        <tbody>
                      {% for user in users %}
    <tr>
    
                            <td class="inbox-small-cells"><i class="fa fa-star"></i></td>
      <td scope="row">{{user.username}}</th>
      <td>{{user.email}}</td>
     
           <td><a href="{{path('user_delete',{'id':user.id})}}" onclick="return confirm('Are you sure you want to delete this account?');"><i class="fas fa-envelope"></i> </a>
           {% if user.transactions != null %}{% else %}
          <a href="{{path('user_delete',{'id':user.id})}}" onclick="return confirm('Are you sure you want to delete this account?');"><i class="fas fa-trash"></i> </a>
         {% endif %} </td>
          
    </tr>
   {% endfor %}
                           
                          
                        </tbody>
                        </table>

   </div>
</div>
</section></section>
  <div class="navigation b  ">
    {{ knp_pagination_render(users) }}
</div>
     {% endblock %}
{% block javascripts %}
<script src="{{ asset('js/main.js') }}"></script>
<script>
function myFunction() {
  alert("Equipment is Now being deleted");
}
</script>

{% endblock %}