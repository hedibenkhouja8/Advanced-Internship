{% extends 'base.html.twig' %}
{% block body %}
<style>
a{color:black;}
h2{color:#485785}
.b{margin-left:700px;
padding-right:400px;
color:red;}
.container {
  max-width: 1000px;
  margin-left: auto;
  margin-right: auto;
  padding-left: 10px;
  padding-right: 10px;
}

</style>
<section id="main-content">
	<section class="wrapper">
		<div class="table-agile-info2">
   
     <h2>These are the other users:</h2>
     <h7>Notice : To delete a user u must delete all his transactions</h7>
       <a href="{{path("security_registration")}}" class="btn btn-outline-info btn-lg float-right" ><i class="fas fa-user-plus"></i>Add User</a>
<br><br><br>{{form_start(form)}}

    <div class="form-row align-items-end">
    <div class="col-sm">
      {{form_row(form.user)}}
    </div>
    
    <div class="form-group">
    <button class=" btn search">
                        <i class="fa fa-search"></i></button>
    
     {{form_end(form)}}
     
    
    <a class="btn search" href='/company/users'>
                        <i class="fa fa-redo"></i></button></a></div>
 </div>
 <div class="table-inbox-wrap ">
                            <table class="table table-inbox table-hover">
                               <thead>
    <tr>
      <th scope="col">Transactions</th>
      <th scope="col">User Name</th>
      <th scope="col">Email</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
                        <tbody>
                      {% for user in users %}
    <tr>
    {% if user.transactions|length <= 0 %}<td> None</td>
                         {% else %}   <td class="inbox-small-cells"> <a href="{{path('company_user_transaction',{'id':user.id})}}" ><i class="fas fa-history" ></i></a> </td>{% endif %}
      <td scope="row">{{user.username}}</th>
      <td>{{user.email}}</td>{% if user.transactions|length > 0 %}<td> {{user.transactions|length }} Transactions</td>{% else %}
           <td>
          0 Transactions&nbsp;&nbsp;
          <a href="{{path('user_delete',{'id':user.id})}}" onclick="return confirm('Are you sure you want to delete this account?');"><i class="fas fa-trash"></i> </a>
        </td>
          {% endif %}
    </tr>
   {% endfor %}
                           
                          
                        </tbody>
                        </table>

   </div>
</div>
</section></section>
  <div class="navigation b  ">
    {{ knp_pagination_render(users) }}
</div>
     {% endblock %}
{% block javascripts %}
<script src="{{ asset('js/main.js') }}"></script>
<script>
function myFunction() {
  alert("Equipment is Now being deleted");
}
</script>

{% endblock %}